<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title> ICEBOX | Registro </title>
    <%-include ('../partials/src') %>
</head>

<body>
    <%-include ('../partials/navBar') %>
        <form action="/register" method="POST" enctype="multipart/form-data" class='user-form' id='register'>
            <div class='register-form-container'>
                <div class='form-title'> Nuevo usuario </div>

                <label for="name" id=name><i class="fas fa-user"></i> Nombre completo </label>
                <!--En el Value: SÍ vino algo en el input, dejarlo, si no dejarlo vacio (null)-->
                <input type="text" name='name' value="<%= locals.oldData ? oldData.name : null %>"
                    placeholder="Escribe tu nombre completo aquí">
                <!--SI en las variables locales vino "errors" Y si en esa variable errores vino un error para el
                        campo name-->
                <% if (locals.errors && errors.name){ %>
                    <div class='text-danger'>
                        <!--De errors que es la variable que llega, yo quiero la propiedad de name el mensaje-->
                        <%= errors.name.msg %>
                    </div>
                    <% } %>


                        <label for="email"><i class="fas fa-envelope"></i> Email </label>
                        <input type="email" name='email' value="<%= locals.oldData ? oldData.email : null %>"
                            placeholder="Escribe tu correo electrónico" id='email'>
                        <% if (locals.errors && errors.email){ %>
                            <div class='text-danger'>
                                <!--De errors que es la variable que llega, yo quiero la propiedad de name el mensaje-->
                                <%= errors.email.msg %>
                            </div>
                            <% } %>


                                <label for="password"><i class="fas fa-key"></i> Contraseña </label>
                                <input type="password" name='password' placeholder="Escribe tu contraseña">
                                <% if (locals.errors && errors.password){ %>
                                    <div class='text-danger'>
                                        <%= errors.password.msg %>
                                    </div>
                                    <% } %>


                                        <label for="image"><i class="fas fa-image"></i> Imagen de perfil </label>
                                        <input type="file" name='image' class='image-text'>
                                        <% if (locals.errors && errors.image){ %>
                                            <div class='text-danger'>
                                                <%= errors.image.msg %>
                                            </div>
                                            <% } %>

                                                <div class='buttons'>
                                                    <button type="submit" class='register-btn'> Iniciar sesión </button>
                                                    <button type="reset" class=reset-btn> Borrar todo </button>
                                                </div>
            </div>

        </form>
        <%-include ('../partials/footer') %>

</body>
<script src="/JS/script.js">
</script>

</html>